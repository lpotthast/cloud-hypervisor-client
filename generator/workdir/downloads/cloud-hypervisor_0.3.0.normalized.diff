diff --git a/downloads/cloud-hypervisor_0.3.0.yaml b/downloads/cloud-hypervisor_0.3.0.normalized.yaml
index 5ed7e9d..3a000df 100644
--- a/downloads/cloud-hypervisor_0.3.0.yaml
+++ b/downloads/cloud-hypervisor_0.3.0.normalized.yaml
@@ -1,15 +1,14 @@
+---
 openapi: 3.0.1
 info:
   title: Cloud Hypervisor API
   description: Local HTTP based API for managing and inspecting a cloud-hypervisor virtual machine.
   license:
     name: Apache 2.0
-    url: http://www.apache.org/licenses/LICENSE-2.0.html
+    url: "http://www.apache.org/licenses/LICENSE-2.0.html"
   version: 0.3.0
-
 servers:
-  - url: http://localhost/api/v1
-
+  - url: "http://localhost/api/v1"
 paths:
   /vmm.ping:
     get:
@@ -21,7 +20,6 @@ paths:
             application/json:
               schema:
                 $ref: "#/components/schemas/VmmPingResponse"
-
   /vmm.shutdown:
     put:
       summary: Shuts the cloud-hypervisor VMM.
@@ -29,7 +27,6 @@ paths:
       responses:
         204:
           description: The VMM successfully shutdown.
-
   /vm.info:
     get:
       summary: Returns general information about the cloud-hypervisor Virtual Machine (VM) instance.
@@ -40,7 +37,6 @@ paths:
             application/json:
               schema:
                 $ref: "#/components/schemas/VmInfo"
-
   /vm.counters:
     get:
       summary: Get counters from the VM
@@ -51,10 +47,9 @@ paths:
             application/json:
               schema:
                 $ref: "#/components/schemas/VmCounters"
-
   /vm.create:
     put:
-      summary: Create the cloud-hypervisor Virtual Machine (VM) instance. The instance is not booted, only created.
+      summary: "Create the cloud-hypervisor Virtual Machine (VM) instance. The instance is not booted, only created."
       operationId: createVM
       requestBody:
         description: The VM configuration
@@ -66,7 +61,6 @@ paths:
       responses:
         204:
           description: The VM instance was successfully created.
-
   /vm.delete:
     put:
       summary: Delete the cloud-hypervisor Virtual Machine (VM) instance.
@@ -74,7 +68,6 @@ paths:
       responses:
         204:
           description: The VM instance was successfully deleted.
-
   /vm.boot:
     put:
       summary: Boot the previously created VM instance.
@@ -84,7 +77,6 @@ paths:
           description: The VM instance successfully booted.
         404:
           description: The VM instance could not boot because it is not created yet
-
   /vm.pause:
     put:
       summary: Pause a previously booted VM instance.
@@ -96,7 +88,6 @@ paths:
           description: The VM instance could not pause because it is not created yet
         405:
           description: The VM instance could not pause because it is not booted.
-
   /vm.resume:
     put:
       summary: Resume a previously paused VM instance.
@@ -108,7 +99,6 @@ paths:
           description: The VM instance could not resume because it is not booted yet
         405:
           description: The VM instance could not resume because it is not paused.
-
   /vm.shutdown:
     put:
       summary: Shut the VM instance down.
@@ -120,7 +110,6 @@ paths:
           description: The VM instance could not shut down because is not created.
         405:
           description: The VM instance could not shut down because it is not started.
-
   /vm.reboot:
     put:
       summary: Reboot the VM instance.
@@ -132,7 +121,6 @@ paths:
           description: The VM instance could not reboot because it is not created.
         405:
           description: The VM instance could not reboot because it is not booted.
-
   /vm.power-button:
     put:
       summary: Trigger a power button in the VM
@@ -144,7 +132,6 @@ paths:
           description: The button could not be triggered because it is not created yet
         405:
           description: The button could not be triggered because it is not booted.
-
   /vm.resize:
     put:
       summary: Resize the VM
@@ -162,7 +149,6 @@ paths:
           description: The VM instance could not be resized because it is not created.
         429:
           description: The VM instance could not be resized because a cpu removal is still pending.
-
   /vm.resize-disk:
     put:
       summary: Resize a disk
@@ -178,7 +164,6 @@ paths:
           description: The disk was successfully resized.
         500:
           description: The disk could not be resized.
-
   /vm.resize-zone:
     put:
       summary: Resize a memory zone
@@ -194,7 +179,6 @@ paths:
           description: The memory zone was successfully resized.
         500:
           description: The memory zone could not be resized.
-
   /vm.add-device:
     put:
       summary: Add a new device to the VM
@@ -216,7 +200,6 @@ paths:
           description: The new device was successfully (cold) added to the VM instance.
         404:
           description: The new device could not be added to the VM instance.
-
   /vm.remove-device:
     put:
       summary: Remove a device from the VM
@@ -232,7 +215,6 @@ paths:
           description: The device was successfully removed from the VM instance.
         404:
           description: The device could not be removed from the VM instance.
-
   /vm.add-disk:
     put:
       summary: Add a new disk to the VM
@@ -254,7 +236,6 @@ paths:
           description: The new disk was successfully (cold) added to the VM instance.
         500:
           description: The new disk could not be added to the VM instance.
-
   /vm.add-fs:
     put:
       summary: Add a new virtio-fs device to the VM
@@ -276,7 +257,6 @@ paths:
           description: The new device was successfully (cold) added to the VM instance.
         500:
           description: The new device could not be added to the VM instance.
-
   /vm.add-generic-vhost-user:
     put:
       summary: Add a new generic vhost-user device to the VM
@@ -298,7 +278,6 @@ paths:
           description: The new device was successfully (cold) added to the VM instance.
         500:
           description: The new device could not be added to the VM instance.
-
   /vm.add-pmem:
     put:
       summary: Add a new pmem device to the VM
@@ -320,7 +299,6 @@ paths:
           description: The new device was successfully (cold) added to the VM instance.
         500:
           description: The new device could not be added to the VM instance.
-
   /vm.add-net:
     put:
       summary: Add a new network device to the VM
@@ -342,7 +320,6 @@ paths:
           description: The new device was successfully (cold) added to the VM instance.
         500:
           description: The new device could not be added to the VM instance.
-
   /vm.add-vsock:
     put:
       summary: Add a new vsock device to the VM
@@ -364,7 +341,6 @@ paths:
           description: The new device was successfully (cold) added to the VM instance.
         500:
           description: The new device could not be added to the VM instance.
-
   /vm.add-vdpa:
     put:
       summary: Add a new vDPA device to the VM
@@ -386,14 +362,13 @@ paths:
           description: The new vDPA device was successfully (cold) added to the VM instance.
         500:
           description: The new vDPA device could not be added to the VM instance.
-
   /vm.add-user-device:
     put:
       requestBody:
         content:
           application/json:
             schema:
-              $ref: '#/components/schemas/VmAddUserDevice'
+              $ref: "#/components/schemas/VmAddUserDevice"
         description: The path of the new device
         required: true
       responses:
@@ -401,14 +376,13 @@ paths:
           content:
             application/json:
               schema:
-                $ref: '#/components/schemas/PciDeviceInfo'
+                $ref: "#/components/schemas/PciDeviceInfo"
           description: The new device was successfully added to the VM instance.
         "204":
           description: The new device was successfully (cold) added to the VM instance.
         "404":
           description: The new device could not be added to the VM instance.
       summary: Add a new userspace device to the VM
-
   /vm.snapshot:
     put:
       summary: Returns a VM snapshot.
@@ -426,7 +400,6 @@ paths:
           description: The VM instance could not be snapshotted because it is not created.
         405:
           description: The VM instance could not be snapshotted because it is not booted.
-
   /vm.coredump:
     put:
       summary: Takes a VM coredump.
@@ -444,14 +417,12 @@ paths:
           description: The VM instance could not be coredumped because it is not created.
         405:
           description: The VM instance could not be coredumped because it is not booted.
-
   /vmm.nmi:
     put:
       summary: Inject an NMI.
       responses:
         204:
           description: The NMI successfully injected.
-
   /vm.restore:
     put:
       summary: Restore a VM from a snapshot.
@@ -467,7 +438,6 @@ paths:
           description: The VM instance was successfully restored.
         404:
           description: The VM instance could not be restored because it is already created.
-
   /vm.receive-migration:
     put:
       summary: Receive a VM migration from URL
@@ -483,7 +453,6 @@ paths:
           description: The VM migration was successfully received.
         500:
           description: The VM migration could not be received.
-
   /vm.send-migration:
     put:
       summary: Send a VM migration to URL
@@ -499,7 +468,6 @@ paths:
           description: The VM migration was successfully sent.
         500:
           description: The VM migration could not be sent.
-
 components:
   schemas:
     VmmPingResponse:
@@ -512,18 +480,19 @@ components:
         version:
           type: string
         pid:
-          type: integer
-          format: int64
+          type: int64
         features:
           type: array
           items:
             type: string
       description: Virtual Machine Monitor information
-
     VmState:
       type: string
-      enum: [Created, Running, Shutdown, Paused]
-
+      enum:
+        - Created
+        - Running
+        - Shutdown
+        - Paused
     VmInfo:
       required:
         - config
@@ -534,16 +503,13 @@ components:
           $ref: "#/components/schemas/VmConfig"
         state:
           $ref: "#/components/schemas/VmState"
-
         memory_actual_size:
-          type: integer
-          format: int64
+          type: int64
         device_tree:
           type: object
           additionalProperties:
             $ref: "#/components/schemas/DeviceNode"
       description: Virtual Machine information
-
     DeviceNode:
       type: object
       properties:
@@ -552,7 +518,6 @@ components:
         resources:
           type: array
           items:
-            # Rust enum type (with data) which can't be better represented here
             type: object
         children:
           type: array
@@ -560,15 +525,12 @@ components:
             type: string
         pci_bdf:
           type: string
-
     VmCounters:
       type: object
       additionalProperties:
         type: object
         additionalProperties:
-          type: integer
-          format: int64
-
+          type: int64
     PciDeviceInfo:
       required:
         - id
@@ -580,7 +542,6 @@ components:
         bdf:
           type: string
       description: Information about a PCI device
-
     PayloadConfig:
       type: object
       properties:
@@ -597,7 +558,6 @@ components:
         host_data:
           type: string
       description: Payloads to boot in guest
-
     VmConfig:
       required:
         - payload
@@ -650,7 +610,7 @@ components:
         user_devices:
           type: array
           items:
-            $ref: '#/components/schemas/UserDeviceConfig'
+            $ref: "#/components/schemas/UserDeviceConfig"
         vdpa:
           type: array
           items:
@@ -686,7 +646,6 @@ components:
           items:
             $ref: "#/components/schemas/LandlockConfig"
       description: Virtual machine configuration
-
     CpuAffinity:
       required:
         - vcpu
@@ -699,13 +658,11 @@ components:
           type: array
           items:
             type: integer
-
     CpuFeatures:
       type: object
       properties:
         amx:
           type: boolean
-
     CpuTopology:
       type: object
       properties:
@@ -717,12 +674,13 @@ components:
           type: integer
         packages:
           type: integer
-
     CoreSchedulingMode:
       type: string
-      enum: ["Vm", "Vcpu", "Off"]
-      default: "Vm"
-
+      enum:
+        - Vm
+        - Vcpu
+        - "Off"
+      default: Vm
     CpusConfig:
       required:
         - boot_vcpus
@@ -753,37 +711,28 @@ components:
           $ref: "#/components/schemas/CpuFeatures"
         core_scheduling:
           $ref: "#/components/schemas/CoreSchedulingMode"
-
-
     PciSegmentConfig:
       required:
         - pci_segment
       type: object
       properties:
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         mmio32_aperture_weight:
-          type: integer
-          format: int32
+          type: int32
         mmio64_aperture_weight:
-          type: integer
-          format: int32
-
+          type: int32
     PlatformConfig:
       type: object
       properties:
         num_pci_segments:
-          type: integer
-          format: int16
+          type: int16
         iommu_segments:
           type: array
           items:
-            type: integer
-            format: int16
+            type: int16
         iommu_address_width_bits:
-          type: integer
-          format: uint8
+          type: uint8
         serial_number:
           type: string
         uuid:
@@ -804,7 +753,6 @@ components:
         vfio_p2p_dma:
           type: boolean
           default: true
-
     MemoryZoneConfig:
       required:
         - id
@@ -814,8 +762,7 @@ components:
         id:
           type: string
         size:
-          type: integer
-          format: int64
+          type: int64
         file:
           type: string
         mergeable:
@@ -828,41 +775,33 @@ components:
           type: boolean
           default: false
         hugepage_size:
-          type: integer
-          format: int64
+          type: int64
         host_numa_node:
-          type: integer
-          format: int32
+          type: int32
         hotplug_size:
-          type: integer
-          format: int64
+          type: int64
         hotplugged_size:
-          type: integer
-          format: int64
+          type: int64
         prefault:
           type: boolean
           default: false
-
     MemoryConfig:
       required:
         - size
       type: object
       properties:
         size:
-          type: integer
-          format: int64
+          type: int64
         hotplug_size:
-          type: integer
-          format: int64
+          type: int64
         hotplugged_size:
-          type: integer
-          format: int64
+          type: int64
         mergeable:
           type: boolean
           default: false
         hotplug_method:
           type: string
-          default: "Acpi"
+          default: Acpi
         shared:
           type: boolean
           default: false
@@ -870,8 +809,7 @@ components:
           type: boolean
           default: false
         hugepage_size:
-          type: integer
-          format: int64
+          type: int64
         prefault:
           type: boolean
           default: false
@@ -882,7 +820,6 @@ components:
           type: array
           items:
             $ref: "#/components/schemas/MemoryZoneConfig"
-
     TokenBucket:
       required:
         - size
@@ -890,30 +827,18 @@ components:
       type: object
       properties:
         size:
-          type: integer
-          format: int64
           minimum: 0
           description: The total number of tokens this bucket can hold.
+          type: int64
         one_time_burst:
-          type: integer
-          format: int64
           minimum: 0
           description: The initial size of a token bucket.
+          type: int64
         refill_time:
-          type: integer
-          format: int64
           minimum: 0
           description: The amount of milliseconds it takes for the bucket to refill.
-      description:
-        Defines a token bucket with a maximum capacity (_size_), an initial burst size
-        (_one_time_burst_) and an interval for refilling purposes (_refill_time_).
-        The refill-rate is derived from _size_ and _refill_time_, and it is the constant
-        rate at which the tokens replenish. The refill process only starts happening after
-        the initial burst budget is consumed.
-        Consumption from the token bucket is unbounded in speed which allows for bursts
-        bound in size by the amount of tokens available.
-        Once the token bucket is empty, consumption speed is bound by the refill-rate.
-
+          type: int64
+      description: "Defines a token bucket with a maximum capacity (_size_), an initial burst size (_one_time_burst_) and an interval for refilling purposes (_refill_time_). The refill-rate is derived from _size_ and _refill_time_, and it is the constant rate at which the tokens replenish. The refill process only starts happening after the initial burst budget is consumed. Consumption from the token bucket is unbounded in speed which allows for bursts bound in size by the amount of tokens available. Once the token bucket is empty, consumption speed is bound by the refill-rate."
     RateLimiterConfig:
       type: object
       properties:
@@ -921,10 +846,7 @@ components:
           $ref: "#/components/schemas/TokenBucket"
         ops:
           $ref: "#/components/schemas/TokenBucket"
-      description:
-        Defines an IO rate limiter with independent bytes/s and ops/s limits.
-        Limits are defined by configuring each of the _bandwidth_ and _ops_ token buckets.
-
+      description: Defines an IO rate limiter with independent bytes/s and ops/s limits. Limits are defined by configuring each of the _bandwidth_ and _ops_ token buckets.
     RateLimitGroupConfig:
       required:
         - id
@@ -935,7 +857,6 @@ components:
           type: string
         rate_limiter_config:
           $ref: "#/components/schemas/RateLimiterConfig"
-
     VirtQueueAffinity:
       required:
         - queue_index
@@ -948,16 +869,20 @@ components:
           type: array
           items:
             type: integer
-
     ImageType:
       type: string
-      enum: ["FixedVhd", "Qcow2", "Raw", "Vhdx", "Unknown"]
-
+      enum:
+        - FixedVhd
+        - Qcow2
+        - Raw
+        - Vhdx
+        - Unknown
     LockGranularity:
       type: string
-      enum: [ByteRange, Full]
+      enum:
+        - ByteRange
+        - Full
       default: ByteRange
-
     DiskConfig:
       type: object
       properties:
@@ -986,11 +911,9 @@ components:
         rate_limiter_config:
           $ref: "#/components/schemas/RateLimiterConfig"
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         id:
           type: string
         serial:
@@ -1011,7 +934,6 @@ components:
           $ref: "#/components/schemas/ImageType"
         lock_granularity:
           $ref: "#/components/schemas/LockGranularity"
-
     NetConfig:
       type: object
       properties:
@@ -1019,11 +941,11 @@ components:
           type: string
         ip:
           type: string
-          default: "192.168.249.1"
+          default: 192.168.249.1
           description: IPv4 or IPv6 address
         mask:
           type: string
-          default: "255.255.255.0"
+          default: 255.255.255.0
           description: Must be a valid IPv4 netmask if ip is an IPv4 address or a valid IPv6 netmask if ip is an IPv6 address.
         mac:
           type: string
@@ -1047,15 +969,13 @@ components:
           type: string
         vhost_mode:
           type: string
-          default: "Client"
+          default: Client
         id:
           type: string
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         rate_limiter_config:
           $ref: "#/components/schemas/RateLimiterConfig"
         offload_tso:
@@ -1067,7 +987,6 @@ components:
         offload_csum:
           type: boolean
           default: true
-
     RngConfig:
       required:
         - src
@@ -1076,25 +995,21 @@ components:
         id:
           type: string
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         iommu:
           type: boolean
           default: false
         src:
           type: string
-
     BalloonConfig:
       required:
         - size
       type: object
       properties:
         size:
-          type: integer
-          format: int64
+          type: int64
         deflate_on_oom:
           type: boolean
           default: false
@@ -1103,7 +1018,6 @@ components:
           type: boolean
           default: false
           description: Enable guest to report free pages.
-
     FsConfig:
       required:
         - num_queues
@@ -1123,14 +1037,11 @@ components:
           type: integer
           default: 1024
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         id:
           type: string
-
     GenericVhostUserConfig:
       required:
         - queue_sizes
@@ -1146,14 +1057,11 @@ components:
           items:
             type: uint16
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         virtio_id:
           type: uint32
-
     PmemConfig:
       required:
         - file
@@ -1162,8 +1070,7 @@ components:
         file:
           type: string
         size:
-          type: integer
-          format: int64
+          type: int64
         iommu:
           type: boolean
           default: false
@@ -1171,18 +1078,20 @@ components:
           type: boolean
           default: false
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         id:
           type: string
-
     ConsoleMode:
       type: string
-      enum: ["Off", "Pty", "Tty", "File", "Socket", "Null"]
-
+      enum:
+        - "Off"
+        - Pty
+        - Tty
+        - File
+        - Socket
+        - "Null"
     ConsoleConfig:
       required:
         - mode
@@ -1200,12 +1109,9 @@ components:
         id:
           type: string
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
-
+          type: uint8
     SerialConfig:
       required:
         - mode
@@ -1217,7 +1123,6 @@ components:
           type: string
         mode:
           $ref: "#/components/schemas/ConsoleMode"
-
     DebugConsoleConfig:
       required:
         - mode
@@ -1229,7 +1134,6 @@ components:
           $ref: "#/components/schemas/ConsoleMode"
         iobase:
           type: integer
-
     DeviceConfig:
       required:
         - path
@@ -1241,22 +1145,17 @@ components:
           type: boolean
           default: false
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         id:
           type: string
         x_nv_gpudirect_clique:
-          type: integer
-          format: int8
+          type: int8
         x_exclude_mmap_bars:
           type: array
           items:
-            type: integer
-            format: int64
-
+            type: int64
     UserDeviceConfig:
       required:
         - socket
@@ -1267,12 +1166,9 @@ components:
         id:
           type: string
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
-
+          type: uint8
     TpmConfig:
       required:
         - socket
@@ -1280,7 +1176,6 @@ components:
       properties:
         socket:
           type: string
-
     VdpaConfig:
       required:
         - path
@@ -1296,14 +1191,11 @@ components:
           type: boolean
           default: false
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         id:
           type: string
-
     VsockConfig:
       required:
         - cid
@@ -1311,25 +1203,21 @@ components:
       type: object
       properties:
         cid:
-          type: integer
-          format: int64
           minimum: 3
           description: Guest Vsock CID
+          type: int64
         socket:
           type: string
-          description: Path to UNIX domain socket, used to proxy vsock connections.
+          description: "Path to UNIX domain socket, used to proxy vsock connections."
         iommu:
           type: boolean
           default: false
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
+          type: uint8
         id:
           type: string
-
     NumaDistance:
       required:
         - destination
@@ -1337,25 +1225,20 @@ components:
       type: object
       properties:
         destination:
-          type: integer
-          format: int32
+          type: int32
         distance:
-          type: integer
-          format: int32
-
+          type: int32
     NumaConfig:
       required:
         - guest_numa_id
       type: object
       properties:
         guest_numa_id:
-          type: integer
-          format: int32
+          type: int32
         cpus:
           type: array
           items:
-            type: integer
-            format: int32
+            type: int32
         distances:
           type: array
           items:
@@ -1367,11 +1250,9 @@ components:
         pci_segments:
           type: array
           items:
-            type: integer
-            format: int32
+            type: int32
         device_id:
           type: string
-
     VmResize:
       type: object
       properties:
@@ -1380,13 +1261,10 @@ components:
           type: integer
         desired_ram:
           description: desired memory ram in bytes
-          type: integer
-          format: int64
+          type: int64
         desired_balloon:
           description: desired balloon size in bytes
-          type: integer
-          format: int64
-
+          type: int64
     VmResizeDisk:
       type: object
       properties:
@@ -1395,9 +1273,7 @@ components:
           type: string
         desired_size:
           description: desired disk size in bytes
-          type: integer
-          format: int64
-
+          type: int64
     VmResizeZone:
       type: object
       properties:
@@ -1405,32 +1281,28 @@ components:
           type: string
         desired_ram:
           description: desired memory zone size in bytes
-          type: integer
-          format: int64
-
+          type: int64
     VmRemoveDevice:
       type: object
       properties:
         id:
           type: string
-
     VmSnapshotConfig:
       type: object
       properties:
         destination_url:
           type: string
-
     VmCoredumpData:
       type: object
       properties:
         destination_url:
           type: string
-
     MemoryRestoreMode:
       type: string
-      enum: [Copy, OnDemand]
+      enum:
+        - Copy
+        - OnDemand
       default: Copy
-
     RestoreConfig:
       required:
         - source_url
@@ -1444,7 +1316,6 @@ components:
           $ref: "#/components/schemas/MemoryRestoreMode"
         resume:
           type: boolean
-
     ReceiveMigrationData:
       required:
         - receiver_url
@@ -1452,16 +1323,13 @@ components:
       properties:
         receiver_url:
           type: string
-
     TimeoutStrategy:
       type: string
-      enum: ["Cancel", "Ignore"]
-      default: "Cancel"
-      description: >
-        The strategy to apply when the migration timeout is reached.
-        Cancel will abort the migration and keep the VM running on the source.
-        Ignore will proceed with the migration regardless of the downtime requirement.
-
+      enum:
+        - Cancel
+        - Ignore
+      default: Cancel
+      description: "The strategy to apply when the migration timeout is reached. Cancel will abort the migration and keep the VM running on the source. Ignore will proceed with the migration regardless of the downtime requirement.\n"
     SendMigrationData:
       required:
         - destination_url
@@ -1472,34 +1340,23 @@ components:
         local:
           type: boolean
         downtime_ms:
-          type: integer
-          format: int64
           minimum: 1
           default: 300
-          description: >
-            The maximum downtime the migration aims for, in milliseconds.
-            Defaults to 300ms.
+          description: "The maximum downtime the migration aims for, in milliseconds. Defaults to 300ms.\n"
+          type: int64
         timeout_s:
-          type: integer
-          format: int64
           minimum: 1
           default: 3600
-          description: >
-            The timeout for the migration (maximum total duration), in seconds.
-            Defaults to 3600s (one hour).
+          description: "The timeout for the migration (maximum total duration), in seconds. Defaults to 3600s (one hour).\n"
+          type: int64
         timeout_strategy:
           $ref: "#/components/schemas/TimeoutStrategy"
         connections:
-          type: integer
-          format: int64
           default: 1
           minimum: 1
           maximum: 128
-          description: >
-            The number of parallel TCP connections to use for migration.
-            Must be between 1 and 128. Multiple connections are not supported
-            with local UNIX-socket migration.
-
+          description: "The number of parallel TCP connections to use for migration. Must be between 1 and 128. Multiple connections are not supported with local UNIX-socket migration.\n"
+          type: int64
     VmAddUserDevice:
       required:
         - socket
@@ -1508,12 +1365,9 @@ components:
         socket:
           type: string
         pci_segment:
-          type: integer
-          format: int16
+          type: int16
         pci_device_id:
-          type: integer
-          format: uint8
-
+          type: uint8
     LandlockConfig:
       required:
         - path
